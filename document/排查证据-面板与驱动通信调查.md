---
tags: [elfin, ros2, 排查, 证据]
date: 2026-08-22
related: "[[排查推测-面板与驱动通信调查]]"
---

# Elfin 面板与驱动通信调查 — 证据记录

> [!abstract] 说明
> 本文记录 2026-08-22 在上次硬件测试遗留现场(实机连接中, 4 组进程存活)上的全部观察证据,
> 每条包含: 终端命令、关键输出、涉及的文件。
> 推测与结论见 [[排查推测-面板与驱动通信调查]], 每条证据末尾回链它支持的推测。

### 证据 1: 机型包删除后的残留引用

```bash
git -C elfin_ws status --short
# → 10 个包整包 D: elfin3/elfin5_l/elfin10/elfin10_l/elfin15 的 *_ros2_gazebo 与 *_ros2_moveit2

grep -rn 'elfin10_ros2_gazebo' elfin_ws/src
# → elfin_basic_api/launch/elfin_basic_api.launch.py:43,51,59 引用已删包(URDF/SRDF/kinematics)

grep -n 'elfin3\|elfin10\|elfin15\|elfin5_l' scripts/start_sim.py scripts/start_real.py
# → 两个脚本的 SIM_COMMANDS / REAL_COMMANDS 仍含被删机型
```

涉及文件: `elfin_ws/src/elfin_robot/elfin_basic_api/launch/elfin_basic_api.launch.py`(已删)、`scripts/start_sim.py`、`scripts/start_real.py`(已清理)、`elfin_ws/build_all.sh`(全量重建, 10 包编译通过, exit=0)

> 支持: [[排查推测-面板与驱动通信调查#推测 1: 删除多余机型包后残留引用导致启动失败 ✅ 证实(已修复)]]

### 证据 2: 实机进程全部以 root 存活

```bash
ps -eo pid,user,rtprio,cmd | grep -E 'ros2|rviz|gazebo|move_group|controller_manager|elfin' | grep -v grep
```

| PID | 用户 | 进程 | 承载节点 |
|---|---|---|---|
| 19123 | root | `ros2_control_node` | `/controller_manager`, `/elfin_hw`(驱动), 各 controller |
| 19119 | root | `static_transform_publisher` | world→elfin_base_link |
| 19360 | root | `rviz2` | `/rviz2` |
| 19362 | root | `move_group` | `/move_group` |
| 19915 | root | `elfin_basic_api_node` | `/elfin_basic_node` |
| 19969 | root | `elfin_gui.py` | `/elfin_gui_node` 等 |
| 15878 | fit | `_ros2_daemon` | ros2 CLI 守护进程 |

> 支持: [[排查推测-面板与驱动通信调查#推测 3: rqt 看不到节点是 sudo 跨用户的 DDS 共享内存(SHM)权限问题 ❌ 证伪]](现场前提)

### 证据 3: ros2 daemon 缓存陈旧

```bash
ros2 node list        # 重启 daemon 前: 缺 /elfin_basic_node、/elfin_gui_node
ros2 daemon stop && ros2 daemon start && sleep 3
ros2 node list        # 重启后: 全部出现, 并提示重名告警(elfin_basic_node x4, rviz2 x2)
```

daemon 进程独立于被观察的 root 进程, 以 fit 身份长期驻留缓存图信息。

> 支持: [[排查推测-面板与驱动通信调查#推测 4: "看不到"的真实原因是环境/缓存/时间窗口三重叠加 ✅ 证实]]

### 证据 4: elfin 服务全部在线(发现层)

```bash
ros2 service list | grep -E 'teleop|reference|end_link|enable|disable|fault|read_d|write_d|motion_state|pos_align'
```

输出(节选): `/joint_teleop` `/cart_teleop` `/stop_teleop` `/home_teleop` `/enable_robot` `/disable_robot` `/clear_fault` `/read_di` `/read_do` `/write_do` `/get_motion_state` `/get_pos_align_state` `/set_reference_link` `/set_end_link` `/elfin_basic_api/enable_robot` `/elfin_basic_api/disable_robot` `/elfin_module_enable_slave2-4` 等, 与源码中的 service 创建点一一对应。

> 支持: [[排查推测-面板与驱动通信调查#推测 2: Servo On 失败是 ROS 通信链路中断 ❌ 证伪]]

### 证据 5: 跨用户话题(tf)数据到达

```bash
source scripts/elfin_env.sh
ros2 topic echo /tf --no-arr      # 后台运行 ≥12s
# → 持续收到 6 个 TransformStamped(6 关节), 发布者为 root 的 robot_state_publisher
```

fit → root 方向的传输无权限障碍, 直接推翻 SHM 权限推测。

> 支持: [[排查推测-面板与驱动通信调查#推测 3: rqt 看不到节点是 sudo 跨用户的 DDS 共享内存(SHM)权限问题 ❌ 证伪]]

### 证据 6: joint_states 约 50Hz 且时间戳抖动

```bash
ros2 topic echo /joint_states --no-arr   # 后台运行, 收集 header.stamp
# → 名义 ~50Hz, 相邻帧间隔在 20ms~300ms 间不规则抖动
ros2 topic info /joint_states
# → Publisher count: 1, Subscription count: 5
```

抖动现象记录, 原因未定位, 列入仿真 debug 观察项。

> 支持: [[排查推测-面板与驱动通信调查#推测 3: rqt 看不到节点是 sudo 跨用户的 DDS 共享内存(SHM)权限问题 ❌ 证伪]](跨用户传输正常的又一例证) / [[排查推测-面板与驱动通信调查#推测 6: /joint_states 时间戳抖动是控制循环被间歇阻塞 ❓ 待查]]

### 证据 7: enable_state 长窗口(60s 以上)后到达

```bash
timeout 4  ros2 topic echo /enable_state   # → 空
# 后台持续 echo ≥60s:
ros2 topic echo /enable_state              # → data: false 连续到达(与 Servo On 失败状态一致)
```

同一 topic, 短窗口为空、长窗口有数据 —— 首次发现+端点匹配耗时 30s 以上是"数据不通"误判的直接来源。

> 支持: [[排查推测-面板与驱动通信调查#推测 4: "看不到"的真实原因是环境/缓存/时间窗口三重叠加 ✅ 证实]] / [[排查推测-面板与驱动通信调查#推测 5: /enable_state 无数据是驱动 status_timer 未被 spin ❌ 证伪]]

### 证据 8: 跨用户 service 调用成功

```bash
ros2 service call /get_motion_state std_srvs/srv/SetBool "{data: true}"
# → success=False, message='true: robot is moving; false: robot is not moving'(机械臂静止, 符合现场)
```

fit 调用 root 驱动的 service 正常应答, 证明面板→驱动的 service 通路完整(只读服务, 对实机安全)。

> 支持: [[排查推测-面板与驱动通信调查#推测 2: Servo On 失败是 ROS 通信链路中断 ❌ 证伪]]

### 证据 9: QoS 与发布者归属

```bash
ros2 topic info -v /enable_state
# → 发布方: 节点 elfin_hw, RELIABLE/VOLATILE
# → 订阅方: _ros2cli_*(echo), BEST_EFFORT/VOLATILE
```

RELIABLE 发布 + BEST_EFFORT 订阅为兼容组合(订阅要求 ≤ 发布提供), QoS 不是障碍; 发布者确认是驱动节点 `elfin_hw`。

> 支持: [[排查推测-面板与驱动通信调查#推测 3: rqt 看不到节点是 sudo 跨用户的 DDS 共享内存(SHM)权限问题 ❌ 证伪]]

### 证据 10: 本机网络与 DNS 环境

```bash
ip -o link show
# → lo UP; enp2s0 UP PROMISC(EtherCAT 占用, 同时有 DHCP 地址 192.168.137.32); tailscale0 UP
getent hosts $(hostname)   # 0.03s, 排除 DNS 解析卡死
ros2 multicast send / ros2 multicast receive
# → 收到 'Hello World!'(经 192.168.137.32 回环), 组播发现通道正常
```

多网卡 + 混杂模式是发现变慢的环境因素; DNS/组播本身正常。

> 支持: [[排查推测-面板与驱动通信调查#推测 4: "看不到"的真实原因是环境/缓存/时间窗口三重叠加 ✅ 证实]]

### 证据 11: 源码层依据(文件与行号)

| 事实 | 文件:行号 |
|---|---|
| 面板 Servo On 调 `/enable_robot`(真机) | `elfin_ws/src/elfin_robot/elfin_basic_api/scripts/elfin_gui.py:305-314` |
| 面板点动调 teleop service | `elfin_gui.py:280-291` |
| 面板订阅状态/关节 topic | `elfin_gui.py:911-921` |
| `enableRobot_cb` 四种返回与 20s 超时 | `elfin_ws/src/elfin_robot/elfin_ethercat_driver/src/elfin_ethercat_driver.cpp:611-673` |
| 驱动 service/topic 创建 | `elfin_ethercat_driver.cpp:222-233` |
| `status_timer_` 100ms → `updateStatus()` | `elfin_ethercat_driver.cpp:236-238, 365-372` |
| IO service(read_di/read_do/write_do) | `elfin_ws/src/elfin_robot/elfin_ethercat_driver/src/elfin_ethercat_io_client.cpp:71-75` |
| 驱动节点靠 `read()` 里 `spin_some` 泵动 | `elfin_ws/src/elfin_robot/elfin_ros_control/src/elfin_hardware_interface.cpp:280` |
| 硬件插件进程内 new 驱动(非消息通信) | `elfin_hardware_interface.cpp:93-96` |
| teleop service → FollowJointTrajectory action | `elfin_ws/src/elfin_robot/elfin_basic_api/src/elfin_teleop_api.cpp:47-56, 116` |
| basic_api 的 service/topic | `elfin_ws/src/elfin_robot/elfin_basic_api/src/elfin_basic_api.cpp:53-77` |

> 支持: [[排查推测-面板与驱动通信调查#推测 2: Servo On 失败是 ROS 通信链路中断 ❌ 证伪]] / [[排查推测-面板与驱动通信调查#推测 5: /enable_state 无数据是驱动 status_timer 未被 spin ❌ 证伪]]

### 证据 12: 测试过程中的干扰项与修正记录

调查初期三组"数据不通"的阴性结果均为测试方法问题, 修正后复测均为阳性:

| 干扰项 | 现象 | 修正 |
|---|---|---|
| `bash -c 'source ... && 命令 &'` | `&` 把整条链丢后台, 前台无环境 | 拆行: `source ...; 命令A & 命令B` |
| `ros2 topic pub \| head` 管道 | Python 块缓冲, kill 后输出丢失 | 后台任务 + 读输出文件 |
| `pkill -f 'chatter_test'` | 匹配到自身命令行, shell 自杀(exit -1) | 用任务管理或精确 PID |
| `ros2 topic echo --once` | Foxy 无此参数(Humble 才有) | `timeout N ... \| head` |
| `ros2 --version` | Foxy 无此参数 | 用 `ros2 doctor` / `ros2 pkg prefix` |

> 支持: [[排查推测-面板与驱动通信调查#推测 4: "看不到"的真实原因是环境/缓存/时间窗口三重叠加 ✅ 证实]]

### 产出文件清单

| 文件 | 用途 |
|---|---|
| `scripts/elfin_env.sh` | source 一次配好 foxy + elfin_ws + ROS_LOCALHOST_ONLY=1 |
| `scripts/root_ros.sh` | root shell / root 单命令, 免手动 source |
| `document/ROS2运行状态排查手册.md` | 排查命令速查与方法论 |
| `document/排查推测-面板与驱动通信调查.md` | 推测与结论(本文姊妹篇) |
| `document/排查证据-面板与驱动通信调查.md` | 本文 |
| ~~`scripts/fastdds_no_shm.xml`~~ | 已删除: SHM 权限推测证伪后回滚 |

> [!note] 相关文档
> - [[排查推测-面板与驱动通信调查]] — 本文全部证据对应的推测与判定
> - [[ROS2运行状态排查手册]] — 排查命令速查与链路全景图
